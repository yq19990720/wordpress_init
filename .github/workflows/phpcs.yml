name: "CI"
on: # rebuild any PRs and main branch changes
  push:
    paths:
      - "**.php"
      - "phpcs.xml"
      - ".github/workflows/phpcs.yml"

jobs:
  phpcs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0 # important!

      # we may use whatever way to install phpcs, just specify the path on the next step
      # however, curl seems to be the fastest
      - name: Install PHP_CodeSniffer
        run: |
          curl -OL https://squizlabs.github.io/PHP_CodeSniffer/phpcs.phar
          php phpcs.phar --version

      - name: Analysing the code with phpcs
        run: |
          php phpcs.phar --config-set default_standard PSR12
          php phpcs.phar wp-includes/ebay/ebay_api.php wp-includes/ebay/currency_config.php wp-includes/ebay/outside_self.php wp-includes/ebay/topay_config.php wp-includes/ebay/sign_utils.php
